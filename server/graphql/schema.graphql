type Habits {
    habit_uid: ID!
    habit_name: String!
    amount: Int!
    unit: String!
}

scalar Date

type Progress {
    progress_uid: String!
    habit_uid: ID!
    habit_date: Date!
    done: Boolean!
}

type Query {
    allHabits: [Habits]!
    allProgress(habit_uid: ID!): [Progress]!
}
