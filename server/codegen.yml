schema: graphql/schema.graphql
generates:
    graphql/resolvers/resolvers-types.ts:
        config:
            useIndexSignature: true
        plugins:
            - typescript
            - typescript-resolvers
